// Local Imports
import { InputIcon } from '../../components';

// Interfaces
export interface ParseInputIconsArgs {
  icons: InputIcon[];
  disabled: boolean;
}

// Elements
export interface IconWrapperProps {
  size?: number;
  disabled?: boolean;
}
